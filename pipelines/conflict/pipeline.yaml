name: build-conflict-test
version: 1.0.0

dependencies:
  - name: lib-core
    version: "^1.0.0"
  - name: lib-core
    version: "^2.0.0"

jobs:
  build:
    runtime: alpine:3.18
    steps:
      - name: test
        run: "echo should never run"
