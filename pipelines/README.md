# Pipeline Files

Put Forge pipeline YAML files in this folder.

Run a pipeline from the project root with:

```bash
forge run pipelines/<pipeline-file>.yaml
```

Follow logs with:

```bash
forge logs <run-id> --follow
```
